syntax = "proto3";

package hank;

// [Internal] Output from an unload plugin request to Hank.
message UnloadPluginOutput {
  // An error message, if there was an error.
  optional string error = 1;
}
