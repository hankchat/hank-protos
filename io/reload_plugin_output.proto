syntax = "proto3";

package hank;

// [Internal] Output from a reload plugin request to Hank.
message ReloadPluginOutput {
  // An error message, if there was an error.
  optional string error = 1;
}
