syntax = "proto3";

import "cron/one_shot.proto";

package hank;

// [Internal] Input to a one shot request to Hank.
message OneShotInput {
  // A one shot to send to Hank.
  hank.cron.OneShotJob one_shot_job = 1;
}
